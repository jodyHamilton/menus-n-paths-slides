   $(document).bind('deck.change', function(event, from, to) {
   	  if (to == 3) {
   	  	// Add menus
   	  }
   	  if (to == 4) {
   	  	// add path
   	  }
   	  if (to == 5) {
   	  	// add breadcrumb
   	  }
   	  if (to > 5) {
   	  	// ensure menus, paths and breadcrumbs are added.
   	  }
    });
